import React from 'react';


const Order = ({order}) =>
{
    console.log(order)

    return order
        ? <> </> : null
    {/* <div className = "order">
        order placeholder
     </div>; */}
};

export default Order;